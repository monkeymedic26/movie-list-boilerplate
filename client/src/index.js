import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App.jsx';

ReactDOM.render(<App />, document.getElementById('app'));

// fork and clone down this repo
// in the command line, type "npm install"
// open two terminal windows, in one window, type "npm start" and in the other window type "npm run build"
// go to localhost:3000 in your browser and you should see the words "hello world" - if you don't, slack me